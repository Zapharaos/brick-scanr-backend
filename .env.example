# Redis Configuration
# Set a strong password for Redis authentication
# This password will be used by both the Redis server and the app service
BRICK_SCANR_REDIS_PASSWORD=changeme123!

# Note: Copy this file to .env and change the password before running docker-compose
# The .env file will be automatically loaded by docker-compose

